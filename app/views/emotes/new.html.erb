<h1>Add an emoticon to the emotejiji library.</h1>

<%= form_for(@emote) do |f| %>
  <% if @emote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emote.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @emote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_field :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', emotes_path %>
